import React from 'react'
import './Sources.scss'
const Sources = (props) => {
    return (
        <div>
            <div className="sources">{props.names}</div>
        </div>
    )
}

export default Sources
